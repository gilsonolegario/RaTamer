# Result — Task 3: README rewrite

Spec: `docs/superpowers/specs/2026-08-08-0.9.0-release-prep-design.md`
Verdict: **implementado**

## What was done

`README.md` rewritten (231 lines) following spec section C structure exactly:
pitch under title → hero screenshot (`screenshots/settings-about.png`) → badges
(Swift + macOS 14+, no license badge) → Features table → Screenshots (all 3
referenced) → Requirements / Permissions / Quick start → How it works (existing
architecture diagram kept verbatim, points to Advanced) → Configuration →
RatTest → Diagnostics → Distribution (section E, 2 paths) → Advanced: the
HID++ protocol (section D deep-dive) → History (Python chapter closed; 0.9.0
entry added, History doubles as changelog per spec). Only `README.md` edited;
no LICENSE created; nothing committed (no git repo).

## Verification

- Facts cross-checked against source: `Protocol.swift` (short 0x10/7B, long
  0x11/20B, body layout), `HIDPPSession.swift` (Root getFeature `[hi,lo,0x00]`,
  index in resp[4], 0 = absent, 3×0.5s retry, response matching incl. softwareID
  own-or-0), `ReprogrammableControls.swift` (0x1B04, function nibbles
  0x00–0x03, flags 0x01/0x02/0x10/0x20, `11 <dev> <feat> 00 <cid×4>` state
  report, `11 <dev> <feat> 10 <dx×4>` raw XY), `HIDLocator.swift`
  (IOServiceGetMatchingServices path, IOHIDManager never enumerates),
  `IOHIDDeviceWrapper.swift` (Output-only writes, single-slot mailbox),
  `BatteryStatus.swift` (0x1000), `AdjustableDPI.swift` (0x2201),
  `SmartShiftControls.swift` (0x2110), `DPICycle.swift` ([1000,1600,2000,4000]),
  `ConfigStore.swift` (config.json schema v2, `ButtonAction.cycleDPI` exists),
  `LoginItem.swift` (SMAppService), `Permissions.swift` (Accessibility only),
  `AboutTabView` (icon + version fallback 0.9.0), `RatDiagnose/main.swift`
  (`--divert <n>`), `RatTest` (Pressed footer + Run). 8-control CID table and
  interface D1/D2/D3 from `.superpowers/sdd/.../progress.md` (line 85 / line
  73); cold-start 0.85s and 0xE0005000 buffer-length finding from the same
  ledgers. No fact cited in the README could not be verified.
- Commands/files cited all exist: `swift run RatTamer`, `swift run RatTest`,
  `swift run RatDiagnose`, `./scripts/build-app.sh`, `Sources/RatTamerCore/HIDPP/`,
  3 screenshots.
- Image paths resolve: `settings-about.png` (hero), `settings-buttons.png`,
  `menu-bar.png` — all `OK`, valid PNGs.
- No license badge, no license mention, no LICENSE file.
- No emojis; English; clean markdown.
- `swift build` green; `swift test` 151/151 passed (matches spec test gate).
  No test runner of the listed kinds (package.json/Cargo.toml/etc.); SwiftPM
  runner used.

## Divergence resolved (mechanical)

- Badge text: spec section C says "Swift 5.9" and "macOS 14+"; task brief
  OUTPUT REQUIREMENTS say only "Swift" and "macOS 14+". Resolved with the
  shields.io convention `![Swift](…Swift-5.9…)` — badge label "Swift", message
  "5.9" (5.9 verified from Package.swift `swift-tools-version`). Both readings
  satisfied; no license badge in either.

## Reuse

Not applicable: documentation-only task (README prose). No implementation
feature that a library could resolve; the spec explicitly mandates no new
dependencies. `reuse-finder` not dispatched.

## Notes

- Stale LSP diagnostics on `ButtonAction.cycleDPI` in test files are index
  noise — the member exists (ConfigStore.swift:66) and `swift build`/`swift test`
  pass.
- Screenshot visual content was captured/validated by the screenshot task
  (spec section F); this task only verifies path resolution (mechanical).
